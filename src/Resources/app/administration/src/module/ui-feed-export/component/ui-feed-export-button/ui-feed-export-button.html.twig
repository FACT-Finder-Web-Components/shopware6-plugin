<div class="ui-feed-export-button">
  <sw-button
            @click="getFeedExportFile('/api/_action/fact-finder/generate-feed')"
{#             @click="getFeedExportFile('{{ path('api.action.fact_finder.export_feed') }}')"#}
             variant="success"
             size="large"
  >
    {{ $tc('ui-feed-export.component.export-button.title') }}
  </sw-button>
{#  <button class="btn is--primary" data-url="{{ path('administration.fact_finder.export_feed') }}">Generate feed</button>#}
</div>
